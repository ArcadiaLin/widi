# runtime-smoke

Placeholder project-local extension root for runtime composition smoke checks.

The current `ExtensionLoader` records roots but does not load file/module extensions yet, so this directory intentionally contains no executable extension.
